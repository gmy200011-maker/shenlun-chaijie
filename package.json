{
  "name": "shenlun-chaijie",
  "version": "1.0.0",
  "description": "浙江宣传文章拆解工具 — 申论/面试备考（前端 React + 后端 Express，单服务部署）",
  "private": true,
  "engines": {
    "node": ">=20.19"
  },
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "postinstall": "npm run build",
    "start": "node server/index.js",
    "dev": "node server/index.js"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "express": "^4.21.0",
    "jsonwebtoken": "^9.0.2"
  }
}
